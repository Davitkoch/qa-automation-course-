//1. String

const myName = "Davit";
const name = "Tohir";
const studentName = "Muhammad";

console.log(name);
console.log(myName);
console.log(studentName);

//2. Numbers

//const birthYear = 1980;
//const carModel = 2024;
//const studentScore = 90.5;

//3. Boollean

//try to start boolian variable names with  with "IS"

//let isLightOn = false; // The is off when you enter the room

//console.log("Is the light on?", isLightOn);

//Later, someone comes and turn on the light

//lightOn = true;

//console.log("Is the light on?", isLightOn);

//4.Null
// in the assigned seats in a party,it is 4 pm  arrived so its full

//let seatOwner = null;

//console.log("seat status", seatOwner);

//party starts at 5pm the seat has an owner

//seatOwner = "Bohdana";

//console.log("Seat status:", seatOwner);

//5.undefined

//Before teacher checking attendance

//let studentPresent;
//console.log("Student present?", studentPresent);

//later, after  checking attendance
//studentPresent = true;

//console.log("Student present?", studentPresent);

//6. Array (non primitive)

// empty aarray
const colors = ["red", "green", "blue"];
console.log(colors);

const studentScore = [70, 85, 88, 95];
console.log(studentScore);

//not reccomended for arrays!! array that has all kind of data types

const array = ["johnathan", 30, true];
console.log(array);

//7. Object (non-primitive)

// left side inside curly braces is keys and the right side is values

const person = {
  name: "alex",
  age: 42,
  isStudent: false,
};

console.log(person);
